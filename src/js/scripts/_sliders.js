const sliders = () => {
	let ayarlar, slide;
	document.addEventListener("DOMContentLoaded", () => {
		ayarlar = {};

		slide = document.querySelector("");
		if (slide) {
		}
	});
};

export default sliders;
