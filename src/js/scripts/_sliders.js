const sliders = () => {
  let settings, slider;
  document.addEventListener('DOMContentLoaded', () => {
    settings = {};

    slider = document.querySelector('');
    if (slider) {
    }
  });
};

export default sliders;
